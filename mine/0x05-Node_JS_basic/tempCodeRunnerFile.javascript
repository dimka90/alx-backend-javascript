function myeach(array, callback)
{
for (i = 0; i<array.length; i++)
    {
        callback[array[i]]
    }
}

courses = ["computer", "English", "BIo"]

myeach(courses, (item) =>{

    console.log("codin")
}) 